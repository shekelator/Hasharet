var calendar = require("../calendar");

describe("Calendar", function() {
	it("returns dates", function(done) {
		expect(3).toBe(3);
		done();
	});

	it("returns dates greater than current date");
	it("returns information on a particular date");

});